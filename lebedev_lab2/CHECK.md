Оценки:
3/3 за сроки
3/3 за основное задание
4/4 за GUI и подпись - по ним нет вопросов

Из комментариев - запустить программу оказалось непросто. Возможно, есть какая-то разница, из какого именно Powershell-а запускать, хотя я пробовал 7.5.4 и он по идее должен работать со всеми этими командами.
Сразу не запустился скрипт для генерации сертификатов через make. Если запускать руками, сами команды работали
Потом были большие проблемы с запуском gcc (в целом, поскольку все равно дальше используется signtool из Microsoft SDK и #include <windows.h>, мне кажется лучше было бы вызывать cl.exe вместо gcc, с gcc бывают проблемы на винде).
Кроме того, перед вызовом компилятора и signtool лучше проверять их наличие на системе, а то будут ошибки вроде таких:
![[./images/check.png]]
В целом, чтобы избежать проблем с компилятором и необходимости его настройки, можно заранее скомпилировать secret.exe и потом добавлять в него строку как ресурс. Для подписи - если подписать не получилось, можно предлагать скачать неподписанный файл. Код сервера будет чуть проще и стабильнее.

Других комментариев нет, сделано быстро (только у меня долго заняла проверка), по реализации тоже все хорошо
