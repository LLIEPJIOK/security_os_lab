Оценки по лабе:
3/3 за основное задание, 3/3 за сроки, 4/4 за дополнительные - есть скрипт с парсингом адреса, есть создание такого же exe.
В моем случае я отключал чуть больше флагов для создания такого exe, при этом не отключал optimization-флаги, но видимо в итоге они дают схожий эффект.

Приложение без проблем запустилось (и позволило проэксплуатировать переполнение буфера) в том числе на wine:
![[./images/check_wine.png]]
Единственное, сразу после перехода на level 13 оно крешнулось, видимо из-за попытки "аккуратно" выйти из jump_level, из стека достался какой-то мусор, который был воспринят как сохраненный адрес возврата.
Можно в jump_level вызывать ExitProcess или TerminateProcess после printf(), чтоб программа не пыталась вернуться в main, куда дороги уже нет.

Еще из комментариев - можно добавить в .gitignore папку с промежуточными файлами билда и файлы *.vcxproj.user, обычно они не нужны. Сам lebedev_lab1.exe лучше оставить, это важная часть самой лабы. В остальном все хорошо :)
